# 1. Length

# Determine the length of the string "These 
# aren't the droids you're looking for.".

message = "These aren't the droids you're looking for."

print(len(message)) # 43

text = ''
print(len(text))    # 0

# To determine the the length of the string
# you use the len function which will return
# an integer and ) for an empty string